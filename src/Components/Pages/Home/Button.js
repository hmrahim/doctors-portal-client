import React from 'react';

const Button = () => {
    return (
        <button className="btn btn-primary text-white my-5 uppercase bg-gradient-to-t from-primary to-secondary">
        Get Started
      </button>
    );
};

export default Button;